integer main()
{
	integer childpid, retval,temp,ppid;
	childpid = Fork();
	if(childpid == -2) then
		retval = Exec("sample.xsm");
	else
		print(childpid);
		ppid=Getppid();
		temp=Wait(ppid);
	endif;
return 0;
}
